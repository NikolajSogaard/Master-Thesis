def setup_llm(
        model: str,
        max_tokens: int | None = None,
        temperature: float = 0.6,
        top_p: float = 0.9,
        respond_as_json: bool = True,
):
    # TODO
    # Write function that instantiates your LLM(s)
    # You'll probably want to extract the API keys from a safe or
    # set it as an env var
    return lambda x: x
